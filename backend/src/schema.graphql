# import * from "./generated/prisma.graphql"

type Mutation {
	createArticle(
		title: String!
  		description: String!
		image: String
		greatImage: String
	): Article!
}

type Query {
	articles: [Article!]!
}